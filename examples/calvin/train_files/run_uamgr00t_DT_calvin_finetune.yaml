# Seer-aligned finetune config for UamGR00T_DT on CALVIN ABC.
#
# K=14 full-architecture finetune setting:
#   history frames  = 14  (kept identical to the pretrained model)
#   future_steps    = 3   (future_offset = 3, per CalvinDT_K14DataConfig)
#   action chunk    = 3   (action_horizon = 3)
#   exec horizon    = 1   (predict 3 steps, execute only 1 before re-query)
#   dual camera     = primary_image + wrist_image
#
# Prerequisite: run pretrain config first, then load pretrained checkpoint
# through trainer.pretrained_checkpoint.
run_id: uamvla_gr00t_dt_calvin_abc_finetune
run_root_dir: playground/Checkpoints
seed: 42
wandb_entity: tancilon1-fudan-university-school-of-management
wandb_project: uamvla
is_debug: false
version_id: "0.21"

framework:
  name: UamGR00T_DT

  obs_image_size: [224, 224]
  qwen_image_size: 224

  # history_frames: total frames fed to Qwen as multi-image input (K-1 history + 1 current).
  # Seer finetune.sh: sequence_length=10, window_size=13 → K=10 to match.
  history_frames: 10

  # exec_horizon=1: predict 3-step chunk but return only 1 step per query
  # to match Seer's inference behavior (re-query at every step).
  exec_horizon: 1

  # use_temporal_markers: insert "Frame t-9:", ..., "Current frame:" labels
  # between image groups in the Qwen prompt for prompt-based timestep structure.
  # Default false; set true to experiment with Seer-style temporal conditioning.
  use_temporal_markers: false

  aux_heads: {}

  # SeerJointDecoder: unified self-attn + Seer block mask (replaces future_branch + action_model).
  seer_joint_decoder:
    # Future reconstruction (MAE ViT decoder)
    num_obs_tokens: 9            # 3×3 semantic tokens per view
    future_weight: 1.0
    patch_size: 16               # 14×14 = 196 target patches
    decoder_dim: 2048
    num_decoder_heads: 16
    num_decoder_blocks: 2

    # Joint self-attention (Seer block mask)
    num_joint_layers: 2          # depth of the joint transformer
    num_heads: 16

    # Action prediction (MLP decoder)
    action_dim: 7
    state_dim: 7
    action_horizon: 3            # Seer action_pred_steps=3
    hidden_dim: 1024
    gripper_loss_ratio: 0.01     # Seer train_utils.py:165

  reduce_in_full_precision: true

datasets:
  vla_data:
    dataset_py: lerobot_datasets

    data_root_dir: datasets
    # Calvin ABC language data with K=10 history + future_rgb at t+3.
    # Seer finetune: sequence_length=10 (window_size=13 = K + future_steps).
    data_mix: calvin_abc_dt_k10

    include_state: true
    gr00t_state_indices: [0, 1, 2, 3, 4, 5, 7]

    obs:
      - video.primary_image
      - video.wrist_image

    state_key: state
    action_key: action

    state_dim: 8
    full_state_dim: 8
    robot_state_dim: 8
    action_dim: 7
    action_horizon: 3

    action_type: delta_qpos

    # DT multi-frame signals read by _pack_sample:
    # num_history_frames controls how many past frames to extract from the
    # combined delta_indices window; future_offset marks the future target.
    num_history_frames: 9       # = history_frames - 1 = K-1 (K=10, Seer finetune)
    future_offset: 3            # t+3 frame → future_rgb

    CoT_prompt: "Your task is {instruction}. To identify the key objects for your task. Locate their bounding boxes in [x1,y1,x2,y2] format."

    per_device_batch_size: 4
    load_all_data_for_training: true
    video_backend: torchvision_av

trainer:
  # Load model weights only and start a new finetuning run from step 0.
  pretrained_checkpoint: /inspire/hdd/global_user/heyu-240108120071/dengqi/UniamVLA/playground/Checkpoints/uamvla_gr00t_dt_calvin_pretrain/final_model/pytorch_model.pt
  is_resume: false

  max_train_steps: 100000
  num_warmup_steps: 5000
  save_interval: 10000
  eval_interval: 100

  learning_rate:
    base: 2.5e-05
    qwen_vl_interface: 1.0e-05
    action_model: 1.0e-04

  lr_scheduler_type: cosine_with_min_lr
  scheduler_specific_kwargs:
    min_lr: 1.0e-06

  freeze_modules: ""

  loss_scale:
    vla: 1.0        # action + future reconstruction
    vlm: 0.0

  max_grad_norm: 1.0
  weight_decay: 0.0
  logging_frequency: 100
  gradient_clipping: 1.0
  gradient_accumulation_steps: 2
  gradient_checkpointing: true

  optimizer:
    name: AdamW
    betas: [0.9, 0.95]
    eps: 1.0e-08
    weight_decay: 1.0e-08
